# Build and packaging dependencies
setuptools>=65.0.0
wheel>=0.37.0

# No runtime dependencies needed - this project uses only Python standard library

# Development dependencies (optional)
pytest>=7.0.0
pytest-cov>=4.0.0
